name: Infrastructure Checks

permissions:
  contents: read

on:
  push:
    branches: [master]
  pull_request:
    types: [opened, synchronize, reopened] # these are the defaults - synchronize means 'commits pushed to PR'

jobs:
  terraform-ci:
    name: Terraform CI
    uses: cccteam/github-workflows/.github/workflows/terraform-ci.yml@5be491dfaa55f4e80e42bbef5dbe7ca7027f612f # v6.1.1
    with:
      terraform_version: '1.14.3'
      upload_sarif: true
    secrets:
      app_id: ${{ secrets.TERRAFORM_APP_ID }}
      app_pem: ${{ secrets.TERRAFORM_APP_PEM }}
    permissions:
      actions: read
      contents: write
      pull-requests: write
      security-events: write